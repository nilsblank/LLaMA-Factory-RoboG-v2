# Mock Model Configuration (for testing)

name: mock_model
_target_: embodied_eval.models.MockModel

# Generation parameters
generation_kwargs:
  temperature: 0.7
  max_tokens: 256
  top_p: 0.9

# Response mode for testing
response_mode: echo  # Options: echo, random, fixed
fixed_response: "This is a mock response."
